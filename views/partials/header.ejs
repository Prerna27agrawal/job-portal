<!DOCTYPE html>
<html lang="en">

<head>
    <title>job_portal</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <!-- <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.4.1/semantic.min.css"> -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.min.css" />
</head>

<body>
    <nav style="background-color: #3a4066;" class="navbar navbar-default navbar-dark bg-dark">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                    aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>

                <a style="color:floralwhite" class="navbar-brand" href="/"><i class="fab fa-hive"></i>WeHire</a>
            </div>
            <div id="navbar" class="collapse navbar-collapse">
                <ul class="nav navbar-nav">
                    <% if(currentUser){ %>
                    <% if(currentUser.isCompany) { %>
                    <li class="nav-item"><a
                            style="color: floralwhite;" href="/company/show">Dashboard</a></li>
                    <% } else { %>
                    <li class="nav-item"><a
                            style="color: floralwhite;" href="/seeker/index">Dashboard</a></li>
                    <% } %>
                    <% } %>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    <% if(!currentUser){ %>
                    <li class="nav-item"><a
                            style="color: floralwhite;" href="/login">Login</a></li>
                    <li class="nav-item"><a
                            style="color: floralwhite;" href="/register">Sign Up</a></li>
                    <% } else { %>
                    <% if(currentUser.isCompany) { %>
                    <li><a style="color: floralwhite;" href="/company/<%= currentUser._id %>/myprofile">Signed In As
                            <%= currentUser.username %></a></li>
                    <% } else { %>
                    <li><a style="color: floralwhite;" href="/seeker/<%= currentUser._id %>/myprofile">Signed In As
                            <%= currentUser.username %></a></li>
                    <% } %>
                    <li><a style="color: floralwhite;" href="/logout">Logout</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>